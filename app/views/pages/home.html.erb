<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>My page for ruby app</title>
        <link rel="stylesheet" type="text/css" href="home.css">
    </head>
    <body>
        <!-- start jumbotron module -->
        <div class="jumbotron">
          <h1>Let's discover together!</h1><br/>
          <% if !logged_in? %>
              <p><a class="btn btn-primary btn-lg" 
                    href="/signup" 
                    role="button">Sign in</a>
              </p>
          <% end %>
        </div> 
        <!-- end jumbotron module -->
        
        <!--start main page articles content -->
        <% @article.each do |article| %>
        <div class="row">
            <div class="col-sm-4 col-md-2">
                <div class="thumbnail">
                  <img src="..." alt="here will be an image soon">
                      <div class="caption" align="center">
                          <%= link_to article.title, article_path(article) %>
                      <div class="article-body">
                          <%= truncate(article.description, length: 100) %>
                      </div>
                      <div class="article-actions">
                          <%= link_to "Open article", article_path(article), class: "btn btn-primary" %>
                      </div>    
                </div>
            </div>
        </div>
        <% end %>
        <!--end main page articles content -->
        
    </body>
</html>